---
GENERATOR:
  PackageName: cgo
  PackageDescription:
  PackageLicense:
  Options:
    SafeStrings: true
  Includes:
    - solana-ffi.h
  FlagGroups:
    - {name: LDFLAGS, flags: ["-L${SRCDIR} -lsolana_ffi"]}

PARSER:
  Defines:
    __has_include_next(x): 1
  IncludePaths:
    - /usr/include
  SourcesPaths:
    - ./cgo/solana-ffi.h

TRANSLATOR:
  Rules:
    function:
      - {action: accept, from: "hello"}
      - {action: accept, from: "unique_pubkey"}
      - {action: accept, from: "program_derived_address"}
    private:
      - {transform: unexport}
    post-global:
      - {transform: export}
      - {load: snakecase}
